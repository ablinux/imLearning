#include<stdio.h>
#include<string.h>

int main()
{
    char str[120];
    printf("Enter the string to find Dublicate in place \n");
    printf("> ");scanf("%s",str);
    char temp;
    int strSize = strlen(str);
    printf("User string is = %s, size = %d\n",str,strSize);
    for(int i=0; i<=strlen(str)-1;i++)
    {

    }
}